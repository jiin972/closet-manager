import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //@ts-ignore
  allowedDevOrigins: ["100.127.64.40", "jiin972-macmini.taildeb984.ts.net"],
};

export default nextConfig;
